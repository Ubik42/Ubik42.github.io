# 工具仓状态盘点（2026-08-25）

本记录用于个人主页内容同步。判断依据为各本地仓库的当前分支、远端同步状态、README、测试素材、截图和宿主验证文档；不把规划中的能力写成现有功能。

| 项目 | Git 状态 | 当前公开能力 | 页面处理 |
| --- | --- | --- | --- |
| `art-pipeline-skill` | `main` 与远端一致，工作区干净 | 工具能力握手、合同校验、只读宿主桥、失败关闭、跨工具恢复 | 取代旧 AI ToolTA 项，明确定位为轻量 Skill |
| `asset-delivery-organizer` | `main` 与远端一致，工作区干净 | 只读扫描、三类规则、dry-run 整理、哈希复检、冲突阻断、回滚、复检和收据 | 新增独立作品，加入四张工作台截图 |
| `maya-scene-checker` | `main` 与远端一致，工作区干净 | Maya 只读扫描、组件证据定位、JSON/Markdown 报告；Maya 2025.3.3 可见宿主验收 | 新增独立作品，加入问题、证据、拦截和报告截图 |
| `maya-garment-preparation` | 远端有 MVP；本地正在完成 0.2.0 中文工作台与 Maya GUI 生命周期证据，尚有未提交文件 | 三类服装输入预检、UV/位置传递、复检、失败回滚、单次 Undo、边界歧义检查 | 新增独立作品，版本和未完成边界如实标注 |
| `unreal-asset-batch-auditor` | `main` 与远端一致，工作区干净 | UE 5.8.1 C++ 采集、Python 编排、中文 Slate 面板、批次/取消/部分失败和 JSON Evidence | 新增独立作品，加入三步真实宿主截图 |
| `MayaScope` | `main` 与远端一致，工作区干净 | 不可变场景快照、依赖/引用调查、Runtime Observatory、Query Kernel、回归基线 | 用当前产品名和真实工作区替换旧 MayaIndieTool 归档文案 |
| `MayaCraft` | `main` 与远端一致，工作区干净 | 中文角色工作区、Rig Graph、Deformation MRI、Retarget、Contact IK 与 Maya GUI 验证 | 更新卡片、详情和四张最新截图 |

## 现行边界

- ArtFlow Agent 是独立 AIGC Agent；Art Pipeline Skill 只负责工具/资产管线审计和能力边界。
- Asset Delivery Organizer、Maya Scene Checker、Maya Garment Preparation 和 Unreal Asset Batch Auditor 是独立生产工具，不嵌套回 Skill 仓库。
- 旧 AIToolTA 截图和“统一母仓/六宿主平台”介绍不再作为现行作品入口。
- 网站中的工具截图来自对应仓库当前文档；模拟协议页面、离屏真实 Widget 和真实宿主截图在详情中分别说明。
